export function validation(files?: object) {
  // if (!files?.size) {
  //   alert("이미지가 없습니다.");
  //   return false;
  // }

  // if (files?.size > 5 * 1024 * 1024) {
  //   alert("파일용량이 너무 큽니다.");
  //   return false;
  // }
}
